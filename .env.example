# Copy to .env.local and fill in real values. Never commit secrets.

# Database (Postgres)
DATABASE_URL=${DATABASE_URL}
TEST_DATABASE_URL=${TEST_DATABASE_URL}
PG_BOSS_SCHEMA=${PG_BOSS_SCHEMA}

# Blob / object storage
# Provider TBD — interface defined in src/server/contracts/storage.ts
STORAGE_BUCKET=${STORAGE_BUCKET}
STORAGE_ACCESS_KEY=${STORAGE_ACCESS_KEY}
STORAGE_SECRET_KEY=${STORAGE_SECRET_KEY}

# Queue
# pg-boss uses DATABASE_URL and stores its metadata in PG_BOSS_SCHEMA

# Worker
# Used to call the separate Playwright worker process
WORKER_ENDPOINT=${WORKER_ENDPOINT}
WORKER_SECRET=${WORKER_SECRET}

# LLM (used rule-first; LLM is a secondary enrichment layer)
ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}

# App
NEXT_PUBLIC_APP_URL=${NEXT_PUBLIC_APP_URL}
NODE_ENV=development
